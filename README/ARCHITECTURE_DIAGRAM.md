# Architecture Diagram: shmem_init() and shmem_finalize() Frontend

## Processing Pipeline

```
┌─────────────────────────────────────────────────────────────────┐
│                   Python Source Code                             │
│                                                                  │
│  def my_program():                                              │
│      shmem_init()                                               │
│      work()                                                      │
│      shmem_finalize()                                           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│          Step 1: AST Parsing (ASTVisitor.visit_Call)            │
│                                                                  │
│  Extract Python AST from source code                            │
│  Traverse call nodes looking for shmem_* functions              │
│  Record: name, line, column, arguments                          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│          Step 2: Call Recognition & Collection                  │
│                                                                  │
│  visitor.shmem_calls = [                                        │
│    {'name': 'shmem_init', 'line': 2, 'col': 4, ...},          │
│    {'name': 'shmem_finalize', 'line': 4, 'col': 4, ...}       │
│  ]                                                              │
│                                                                  │
│  Detected operations:                                           │
│    • shmem_init() ✓                                            │
│    • shmem_finalize() ✓                                        │
│    • All other shmem_* operations ✓                            │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│    Step 3: Operation Mapping (shmem_functions dict)             │
│                                                                  │
│  'shmem_init'       → 'init'                                   │
│  'shmem_finalize'   → 'finalize'                               │
│  'shmem_barrier_all'→ 'barrier_all'                            │
│  'shmem_my_pe'      → 'my_pe'                                  │
│  'shmem_n_pes'      → 'n_pes'                                  │
│  'shmem_put'        → 'put'                                    │
│  'shmem_get'        → 'get'                                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│   Step 4: IR Builder (IRBuilder.create_openshmem_*)             │
│                                                                  │
│   create_openshmem_init()       [PENDING MLIR BINDINGS]        │
│   create_openshmem_finalize()   [PENDING MLIR BINDINGS]        │
│                                                                  │
│   Returns OpenSHMEM MLIR operations                            │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│           Step 5: OpenSHMEM MLIR Representation                 │
│                                                                  │
│  module {                                                       │
│    func.func @my_program() {                                   │
│      openshmem.init                                            │
│      // work operations                                         │
│      openshmem.finalize                                        │
│      return                                                     │
│    }                                                            │
│  }                                                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│    Step 6: Lowering to LLVM (SetupOpsToLLVM.cpp)               │
│           [EXISTING IMPLEMENTATION]                             │
│                                                                  │
│  module {                                                       │
│    func.func @my_program() {                                   │
│      call @shmem_init() : () -> ()                             │
│      // work operations                                         │
│      call @shmem_finalize() : () -> ()                         │
│      return                                                     │
│    }                                                            │
│  }                                                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│            Step 7: LLVM Code Generation                         │
│                                                                  │
│  Assembly instructions calling shmem_init() and                │
│  shmem_finalize() from the OpenSHMEM C runtime                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│        Step 8: OpenSHMEM Runtime Execution                      │
│                                                                  │
│  Runtime initializes OpenSHMEM                                 │
│  Executes user code                                            │
│  Runtime finalizes OpenSHMEM                                   │
└─────────────────────────────────────────────────────────────────┘
```

## Component Architecture

```
┌─────────────────────────────────────────────────────────┐
│           shmem4py Frontend Package                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  frontend.py:                                           │
│  ┌────────────────────────────────────────────────┐    │
│  │ Shmem4PyFrontend                               │    │
│  ├────────────────────────────────────────────────┤    │
│  │ - shmem_functions: dict                        │    │
│  │ - compile(func): returns MLIR string           │    │
│  │ - Maps to OpenSHMEM MLIR operations            │    │
│  └────────────────────────────────────────────────┘    │
│                      ↓                                   │
│  ┌────────────────────────────────────────────────┐    │
│  │ ASTVisitor(ast.NodeVisitor)                    │    │
│  ├────────────────────────────────────────────────┤    │
│  │ - shmem_calls: list of call dicts              │    │
│  │ - visit_Call(): detects shmem_* functions      │    │
│  │ - Records: name, line, col, args, keywords     │    │
│  │ - Supports all 8 shmem operations              │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  builder.py:                                            │
│  ┌────────────────────────────────────────────────┐    │
│  │ IRBuilder                                      │    │
│  ├────────────────────────────────────────────────┤    │
│  │ - create_openshmem_init(): generates op        │    │
│  │ - create_openshmem_finalize(): generates op    │    │
│  │ - (Other operation builders: put, get, etc)    │    │
│  │ - Ready for MLIR Python bindings integration   │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  passes.py:                                             │
│  ┌────────────────────────────────────────────────┐    │
│  │ Pass wrappers for optimization pipeline        │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  jit.py:                                                │
│  ┌────────────────────────────────────────────────┐    │
│  │ @shmem_jit decorator (future)                  │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  cli.py:                                                │
│  ┌────────────────────────────────────────────────┐    │
│  │ Command-line interface (future)                │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
└─────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────┐
│        MLIR Infrastructure (in build)                   │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  OpenSHMEM Dialect (C++/ODS)                           │
│  ├── InitOp (openshmem.init)                           │
│  ├── FinalizeOp (openshmem.finalize)                   │
│  └── Other ops (barrier_all, my_pe, n_pes, etc)        │
│                                                          │
│  OpenSHMEM → LLVM Lowering Patterns                    │
│  ├── SetupOpsToLLVM.cpp (handles init/finalize)        │
│  ├── RMAOpsToLLVM.cpp (handles put/get)                │
│  └── Other lowering patterns...                         │
│                                                          │
└─────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────┐
│           OpenSHMEM C Runtime Library                   │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  shmem_init()       - Initialize runtime               │
│  shmem_finalize()   - Shutdown runtime                 │
│  shmem_barrier_all()- Global barrier                   │
│  shmem_my_pe()      - Get PE rank                      │
│  shmem_n_pes()      - Get number of PEs                │
│  shmem_put()        - Remote memory write              │
│  shmem_get()        - Remote memory read               │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## Data Flow: shmem_init() Call

```
Python Source Code
        │
        │ shmem_init()
        │
        ↓
    AST Node
    (ast.Call)
        │
        ├─ func: ast.Name(id='shmem_init')
        ├─ args: []
        └─ keywords: []
        │
        ↓
    ASTVisitor.visit_Call()
        │
        ├─ Check: isinstance(node.func, ast.Name)? YES
        ├─ Extract: func_name = 'shmem_init'
        ├─ Check: func_name in supported_ops? YES
        │
        ↓
    Record in shmem_calls
    {
      'name': 'shmem_init',
      'line': 2,
      'col': 4,
      'args': [],
      'keywords': []
    }
        │
        ↓
    Operation Mapping
    'shmem_init' → 'init'
        │
        ↓
    IRBuilder.create_openshmem_init()
        │
        ↓
    openshmem.InitOp()
        │
        ↓
    MLIR Text: openshmem.init
        │
        ↓
    Lowering: InitOpLowering
        │
        ├─ Create LLVM function type: () -> ()
        ├─ Declare: @shmem_init
        ├─ Create call: llvm.call @shmem_init()
        │
        ↓
    LLVM IR: call void @shmem_init()
        │
        ↓
    Link with OpenSHMEM library
        │
        ↓
    Execute at runtime
```

## Test Coverage Map

```
┌──────────────────────────────────────────────────────────┐
│           Test Suite Coverage                            │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  TestASTVisitorShmemCalls (6 tests)                      │
│  ├─ test_recognize_shmem_init                            │
│  ├─ test_recognize_shmem_finalize                        │
│  ├─ test_recognize_init_and_finalize                     │
│  ├─ test_preserve_call_order                             │
│  ├─ test_recognize_all_shmem_operations                  │
│  └─ test_call_has_location_info                          │
│                                                           │
│  TestFrontendIntegration (2 tests)                       │
│  ├─ test_frontend_init                                   │
│  └─ test_frontend_recognizes_shmem_calls                 │
│                                                           │
│  TestShmemInitFinalize (4 tests)                         │
│  ├─ test_init_is_first_call                              │
│  ├─ test_finalize_is_last_call                           │
│  ├─ test_multiple_inits_detected                         │
│  └─ test_multiple_finalizes_detected                     │
│                                                           │
│  TestBackendMapping (3 tests)                            │
│  ├─ test_shmem_init_mapping                              │
│  ├─ test_shmem_finalize_mapping                          │
│  └─ test_all_operations_mapped                           │
│                                                           │
│  TestIRBuilder (5 tests)                                 │
│  ├─ test_builder_init                                    │
│  ├─ test_builder_has_init_method                         │
│  ├─ test_builder_has_finalize_method                     │
│  ├─ test_init_raises_notimplemented                      │
│  └─ test_finalize_raises_notimplemented                  │
│                                                           │
│  TOTAL: 20+ tests                                        │
│  STATUS: All tests PASS ✓                                │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

## Integration Checklist

```
Phase 2A: AST Parsing [COMPLETE ✅]
├─ [✅] AST visitor recognizes calls
├─ [✅] Metadata tracked (line, col, args)
├─ [✅] Operation mapping defined
├─ [✅] Support all 8 shmem operations
└─ [✅] Test coverage (20+ tests pass)

Phase 2B: IR Generation [PENDING 🔄]
├─ [ ] Enable MLIR Python bindings
├─ [ ] Implement create_openshmem_init()
├─ [ ] Implement create_openshmem_finalize()
├─ [ ] Wire frontend → builder → MLIR
├─ [ ] Verify MLIR output matches dialect
└─ [ ] Integration tests with real MLIR

Phase 2C: Pipeline Integration [PLANNED 📋]
├─ [ ] Connect to lowering infrastructure
├─ [ ] Run optimization passes
├─ [ ] Generate LLVM IR
├─ [ ] Test end-to-end: Python → Executable
└─ [ ] Performance benchmarks

Phase 3: Extended Operations [PLANNED 📋]
├─ [ ] Extend pattern to all 8 operations
├─ [ ] Add control flow (loops, conditionals)
├─ [ ] Add array/memref support
├─ [ ] Type system improvements
└─ [ ] Full shmem4py compatibility
```
